# Insurance_Amount_Prediction
We trained an insurance dataset (train.csv) using three machine learning models: KNN, XGBoost Regression, and Random Forest. Among these, the RF model achieved the highest accuracy.  Therefore, we selected the RF model, saved it as a pickle file, and integrated it into our Insurance IQ web, allowing users to easily estimate their insurance premium.
